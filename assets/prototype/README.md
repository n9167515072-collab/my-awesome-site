# assets/prototype

AI-generated art-direction comps referenced by `DESIGN.md`'s Asset Usage
table — prototype-only, never production truth. See `DESIGN.md`'s "Asset
fidelity notice" before using anything here.

**The actual image files are not yet committed here** — they exist only
as chat attachments from the design session that produced them, and this
session has no filesystem access to save them directly. Before using this
folder to drive prototype implementation, add the actual files (the same
upload flow used earlier in this project works: a zip attachment) named
to match `DESIGN.md`'s Asset Usage table:

- `alice-hero-scene.*` — Alice tabletop comp (box + card + back), used
  without its key/rose-petal props per the fidelity notice.
- `alice-macro-detail.*` — macro corner of the Alice card.
- `vitraji-hero-scene.*` — Vitraji 3-cards + back comp.
- `vitraji-macro-detail.*` — macro "XIX Солнце" comp (content unverified,
  light/framing reference only — see `DESIGN.md`).
- `digital-to-physical-comp.*` — the split "art file vs. physical" comp
  (has a confirmed text defect on the box — see `DESIGN.md`).

No file for the future-deck/CTA scene exists — that's a real gap, not an
oversight, per `DESIGN.md`.
